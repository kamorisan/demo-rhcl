apiVersion: mcp.kuadrant.io/v1alpha1
kind: MCPServerRegistration
metadata:
  name: test-server1-upstream
  namespace: mcp-upstream-test
  labels:
    mcp.kuadrant.io/managed: 'true'
spec:
  path: /mcp
  toolPrefix: test1_
  targetRef:
    group: gateway.networking.k8s.io
    kind: HTTPRoute
    name: mcp-server1-route
