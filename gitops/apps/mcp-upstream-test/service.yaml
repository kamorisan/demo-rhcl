apiVersion: v1
kind: Service
metadata:
  name: mcp-test-server1
  namespace: mcp-upstream-test
  labels:
    app: mcp-test-server1
spec:
  ports:
  - port: 9090
    protocol: TCP
    targetPort: 9090
    name: http
  selector:
    app: mcp-test-server1
