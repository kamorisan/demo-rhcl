# Override base gateway-public-certificate.yaml
#
# The base defines rhcl-gw-public-tls Certificate for *.apps.cluster domains,
# but we use the existing cert-manager-ingress-cert wildcard certificate instead.
#
# This empty list overrides the base definition.
# Only external-api Certificate is needed (defined in certificate-external-sandbox.yaml)
apiVersion: v1
kind: List
items: []
